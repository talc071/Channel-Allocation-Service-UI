# Base URL for the FastAPI backend.
# Leave empty to rely on the Vite dev proxy (recommended for local dev).
# In production, set this to the full URL of the deployed backend, e.g. https://api.example.com
VITE_API_BASE_URL=
